# My GitHub Composite Actions

- In the caller if the folder only contains a file called actions.yml then we only need to pass the folder name
- however if it is called something else we need to change the name of the called to `dir/file.yml`
- Make sure that you allow this repo actions to have access in the other repos by the same user, in the repo settings